[project]
name = "agentic-ai-langgraph-demo"
version = "0.1.0"
description = "Minimal LangGraph + Claude agentic AI demo for Cloud/DevOps engineers"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.2",
    "langchain-anthropic>=0.3",
    "python-dotenv>=1.0",
]
