# In-memory knowledge base — no network calls, always deterministic.
# Keys are (topic_lower, category_lower) for best practices
# and (service_lower, rule_type_lower) for compliance rules.

BEST_PRACTICES: dict[tuple[str, str], str] = {
    ("s3", "security"): (
        "1. Enable S3 Block Public Access at both account and bucket level.\n"
        "2. Enforce HTTPS-only access via bucket policy using the aws:SecureTransport condition "
        "   to deny all HTTP requests.\n"
        "3. Enable SSE-KMS encryption with a customer-managed key (CMK) stored in AWS KMS; "
        "   avoid SSE-S3 for sensitive data.\n"
        "4. Enable S3 server access logging to a dedicated audit bucket in a separate account.\n"
        "5. Enable S3 Object Versioning and MFA Delete for buckets holding critical or "
        "   compliance-required data.\n"
        "6. Use VPC endpoints (Gateway type, no cost) so EC2 and Lambda access S3 over the "
        "   private AWS network rather than the public internet.\n"
        "7. Write least-privilege bucket policies — never use a wildcard (*) principal.\n"
        "8. Enable Amazon Macie to automatically detect and alert on sensitive data (PII, "
        "   credentials) stored in S3.\n"
        "9. Use S3 Object Lock (WORM mode) for regulatory workloads requiring immutability.\n"
        "10. Rotate CMK key material annually and set key deletion windows >= 30 days."
    ),
    ("s3", "cost"): (
        "1. Use S3 Intelligent-Tiering for objects with unpredictable access patterns; it "
        "   moves objects automatically between tiers with no retrieval fees.\n"
        "2. Set S3 Lifecycle policies to transition infrequently accessed objects to S3-IA "
        "   after 30 days, and to Glacier after 90 days.\n"
        "3. Enable S3 Storage Lens to get account-wide visibility into storage usage and "
        "   activity metrics.\n"
        "4. Expire incomplete multipart uploads after 7 days using a Lifecycle rule.\n"
        "5. Delete old versions of objects after a retention window using versioning lifecycle rules."
    ),
    ("iam", "security"): (
        "1. Apply least-privilege: start with zero permissions and grant only what is needed.\n"
        "2. Never attach AdministratorAccess in production; use permission boundaries to cap "
        "   maximum permissions for delegated roles.\n"
        "3. Enforce MFA for all IAM users with console access; use virtual MFA or hardware tokens.\n"
        "4. Use IAM roles for all compute (EC2 instance profiles, Lambda execution roles, "
        "   ECS task roles) — never embed long-lived access keys in code or AMIs.\n"
        "5. Rotate IAM access keys every 90 days; prefer migrating to Secrets Manager or "
        "   short-lived credentials from AWS STS AssumeRole.\n"
        "6. Enable IAM Access Analyzer to surface resources shared outside your account or org.\n"
        "7. Use AWS Organizations SCPs to deny risky actions (disable CloudTrail, leave org, "
        "   create root access keys) across all member accounts.\n"
        "8. Review unused roles and policies quarterly using IAM credential reports and the "
        "   last-used timestamps on policies.\n"
        "9. Prefer AWS-managed policies only as a starting baseline; write customer-managed "
        "   policies scoped to specific resources and conditions.\n"
        "10. Enable CloudTrail in all regions with log file validation and delivery to an "
        "    immutable S3 bucket in a dedicated log-archive account."
    ),
    ("vpc", "security"): (
        "1. Use private subnets for all compute resources (EC2, RDS, ECS, Lambda-VPC); "
        "   place only internet-facing load balancers in public subnets.\n"
        "2. Restrict security group inbound rules to the minimum required source CIDRs or "
        "   security group IDs — never allow 0.0.0.0/0 on management ports.\n"
        "3. Enable VPC Flow Logs (at VPC level) delivered to S3 or CloudWatch Logs for "
        "   network traffic audit and anomaly detection.\n"
        "4. Use NACLs as a stateless defense-in-depth layer; deny known-bad CIDRs at the "
        "   subnet boundary in addition to security group rules.\n"
        "5. Use AWS PrivateLink and VPC interface endpoints for AWS service access; avoids "
        "   traffic leaving the VPC to the public internet.\n"
        "6. Deploy AWS Network Firewall or a third-party NGFW for east-west (VPC-to-VPC) "
        "   inspection in a hub-spoke Transit Gateway architecture.\n"
        "7. Use separate VPCs per environment (dev/staging/prod); connect with Transit Gateway "
        "   rather than VPC peering at scale.\n"
        "8. Enable DNS hostnames and DNS resolution in the VPC; use Route 53 private hosted "
        "   zones for internal service discovery."
    ),
    ("ec2", "security"): (
        "1. Enforce IMDSv2 (token-based metadata service) on all instances to prevent SSRF "
        "   attacks that abuse the instance metadata endpoint.\n"
        "2. Manage patching centrally with AWS Systems Manager Patch Manager; never rely on "
        "   SSH-based manual patching.\n"
        "3. Use AWS Systems Manager Session Manager for interactive shell access — eliminates "
        "   the need for SSH, bastion hosts, and inbound port 22 security group rules.\n"
        "4. Enable EBS encryption by default at the account level; use KMS CMKs for sensitive "
        "   workloads.\n"
        "5. Attach least-privilege IAM instance profiles — scope permissions to the specific "
        "   S3 buckets, SSM parameters, and secrets the instance actually needs.\n"
        "6. Enable Amazon Inspector for continuous vulnerability scanning of EC2 instances "
        "   and container images.\n"
        "7. Build hardened AMIs from a golden image pipeline (EC2 Image Builder); do not "
        "   install software manually on running instances.\n"
        "8. Use dedicated tenancy or bare-metal instance types for workloads with strict "
        "   hardware isolation compliance requirements."
    ),
    ("rds", "security"): (
        "1. Deploy RDS instances in private subnets; set PubliclyAccessible=false; never "
        "   expose the database endpoint to the internet.\n"
        "2. Enable encryption at rest using KMS CMKs — note this must be set at creation "
        "   time; you cannot enable it on an existing unencrypted instance without a snapshot restore.\n"
        "3. Force SSL/TLS connections: set the rds.force_ssl parameter to 1 for PostgreSQL "
        "   and MySQL; use the require_secure_transport system variable for Aurora MySQL.\n"
        "4. Use IAM database authentication for Aurora and RDS MySQL/PostgreSQL — eliminates "
        "   long-lived database passwords; tokens expire after 15 minutes.\n"
        "5. Rotate master credentials automatically via AWS Secrets Manager with the RDS "
        "   rotation Lambda; set rotation interval to 30 days.\n"
        "6. Enable automated backups (retention >= 7 days) and point-in-time recovery (PITR).\n"
        "7. Enable Enhanced Monitoring (1-second granularity) and Performance Insights for "
        "   operational visibility.\n"
        "8. Use RDS Proxy for Lambda-to-RDS connections to pool connections and reduce "
        "   cold-start connection exhaustion."
    ),
    ("lambda", "security"): (
        "1. Apply a least-privilege execution role per Lambda function — one function, one "
        "   scoped role; never share a role across multiple functions with different permissions.\n"
        "2. Store secrets in AWS Secrets Manager or SSM Parameter Store (SecureString); "
        "   never put credentials in environment variables or function code.\n"
        "3. Validate and sanitize all input — Lambda functions are often a public API boundary "
        "   and the first target for injection attacks.\n"
        "4. Enable AWS X-Ray active tracing for all functions to get distributed tracing "
        "   across service calls.\n"
        "5. Set reserved concurrency to prevent a single function from consuming all regional "
        "   capacity and causing a denial of service to other functions.\n"
        "6. Use Lambda Layers for shared dependencies; scan layer contents with Amazon "
        "   Inspector or a third-party SCA tool.\n"
        "7. Enable VPC attachment only when the function genuinely needs access to private "
        "   resources — it adds cold-start latency and requires ENI capacity planning.\n"
        "8. Set function timeout to the minimum practical value; an over-generous timeout "
        "   extends blast radius and cost in runaway scenarios."
    ),
    ("terraform", "state management"): (
        "1. Store Terraform state remotely in an S3 bucket with versioning enabled — never "
        "   commit .tfstate files to git.\n"
        "2. Enable DynamoDB state locking (LockID table) to prevent concurrent terraform apply "
        "   operations from corrupting state.\n"
        "3. Enable S3 server-side encryption (SSE-KMS) on the state bucket — state files "
        "   contain resource IDs, ARNs, and sometimes sensitive output values.\n"
        "4. Use a separate AWS account or at minimum a separate S3 bucket per environment "
        "   (dev/staging/prod) to prevent state file cross-contamination.\n"
        "5. Use Terraform workspaces for lightweight environment separation within the same "
        "   configuration; use separate root modules for true environment isolation.\n"
        "6. Use terraform_remote_state data sources (read-only) for cross-module references "
        "   rather than hard-coding resource IDs.\n"
        "7. Grant the CI/CD pipeline's IAM role least-privilege access to the state bucket "
        "   and DynamoDB table; use separate roles for plan (read-only) and apply.\n"
        "8. Enable S3 access logging and CloudTrail data events on the state bucket to audit "
        "   who read or modified state and when."
    ),
    ("kubernetes", "security"): (
        "1. Enable RBAC and disable anonymous access to the API server; bind ServiceAccounts "
        "   to Roles scoped to the minimum required verbs and resources.\n"
        "2. Use NetworkPolicy resources (or a CNI plugin that enforces them, e.g. Cilium) "
        "   to implement default-deny ingress/egress between namespaces.\n"
        "3. Run containers as non-root (runAsNonRoot: true); set readOnlyRootFilesystem: true "
        "   and drop all Linux capabilities except those explicitly needed.\n"
        "4. Scan container images with Trivy or ECR image scanning in your CI pipeline; "
        "   block images with critical CVEs from reaching production.\n"
        "5. Enable Pod Security Admission (PSA) at the namespace level — use 'restricted' "
        "   profile for production workloads.\n"
        "6. Enable API server audit logging; ship logs to an immutable destination outside "
        "   the cluster (e.g. S3 via Fluent Bit).\n"
        "7. Use IRSA (IAM Roles for Service Accounts) on EKS instead of node-level instance "
        "   profiles so each pod gets scoped, short-lived AWS credentials.\n"
        "8. Regularly rotate service account tokens; set automountServiceAccountToken: false "
        "   on pods that do not need API server access."
    ),
}

COMPLIANCE_RULES: dict[tuple[str, str], str] = {
    ("s3", "cis"): (
        "CIS AWS Foundations Benchmark — S3 controls:\n"
        "  2.1.1  Ensure S3 Bucket is not publicly accessible (Block Public Access enabled).\n"
        "  2.1.2  Ensure S3 Bucket Policy denies HTTP requests "
        "         (aws:SecureTransport condition, Effect: Deny).\n"
        "  2.1.4  Ensure S3 Buckets have object-level CloudTrail logging enabled "
        "         (GetObject, PutObject, DeleteObject data events).\n"
        "  2.1.5  Ensure S3 Block Public Access is configured at the account level in addition "
        "         to individual bucket settings.\n"
        "  2.1.6  Ensure MFA Delete is enabled on S3 buckets storing sensitive or versioned data."
    ),
    ("s3", "well_architected"): (
        "AWS Well-Architected Framework — S3 relevant pillars:\n"
        "  Security   SEC 8  — Protect data at rest: enable SSE-KMS with CMK on all sensitive buckets.\n"
        "  Security   SEC 9  — Protect data in transit: enforce HTTPS; deny HTTP via bucket policy.\n"
        "  Security   SEC 10 — Protect data in use: use Macie to detect sensitive data exposure.\n"
        "  Reliability REL 9  — Enable versioning and cross-region replication for critical data buckets.\n"
        "  Ops Excel  OPS 11 — Enable S3 server access logging and deliver to a separate audit bucket."
    ),
    ("s3", "nist"): (
        "NIST SP 800-53 Rev 5 — S3 relevant controls:\n"
        "  SC-8   Transmission Confidentiality: enforce TLS; deny HTTP requests.\n"
        "  SC-28  Protection of Information at Rest: encrypt with KMS CMK.\n"
        "  AU-2   Event Logging: enable CloudTrail data events for S3 object operations.\n"
        "  AC-3   Access Enforcement: apply least-privilege bucket policies.\n"
        "  CP-9   Information System Backup: enable versioning and cross-region replication."
    ),
    ("iam", "cis"): (
        "CIS AWS Foundations Benchmark — IAM controls:\n"
        "  1.1   Avoid using the root account; enable MFA on the root user.\n"
        "  1.4   Ensure no access keys exist for the root account.\n"
        "  1.7   Eliminate use of root account for everyday administrative tasks.\n"
        "  1.10  Ensure MFA is enabled for all IAM users with console access.\n"
        "  1.12  Ensure credentials unused for >= 90 days are disabled.\n"
        "  1.14  Ensure access keys are rotated every 90 days or less.\n"
        "  1.15  Ensure IAM policies are attached only to groups or roles, not directly to users.\n"
        "  1.16  Ensure IAM policies that allow full '*:*' administrative privileges are not created."
    ),
    ("iam", "well_architected"): (
        "AWS Well-Architected Framework — IAM relevant pillars:\n"
        "  Security SEC 2 — Grant least privilege; use IAM roles for EC2/Lambda/ECS.\n"
        "  Security SEC 3 — Use SCPs in AWS Organizations to restrict service usage org-wide.\n"
        "  Security SEC 6 — Detect threats: enable CloudTrail, GuardDuty, and IAM Access Analyzer.\n"
        "  Security SEC 1 — Separate duties using multiple AWS accounts per environment."
    ),
    ("vpc", "cis"): (
        "CIS AWS Foundations Benchmark — VPC/networking controls:\n"
        "  5.2   Ensure no security group allows unrestricted ingress on SSH (port 22).\n"
        "  5.3   Ensure no security group allows unrestricted ingress on RDP (port 3389).\n"
        "  5.4   Ensure the default security group of every VPC restricts all traffic.\n"
        "  3.9   Ensure VPC Flow Logs are enabled in all VPCs.\n"
        "  3.10  Ensure AWS Config is enabled in all regions (for VPC change detection)."
    ),
    ("ec2", "cis"): (
        "CIS AWS Foundations Benchmark — EC2 controls:\n"
        "  2.2.1 Ensure EBS volume encryption is enabled by default in all regions.\n"
        "  5.6   Ensure EC2 IMDSv2 is enforced (hop limit 1, token required).\n"
        "  Note: Use AWS Config rule 'ec2-imdsv2-check' to detect non-compliant instances.\n"
        "  Note: Use AWS Config rule 'encrypted-volumes' to detect unencrypted EBS volumes."
    ),
    ("rds", "cis"): (
        "CIS AWS Foundations Benchmark — RDS controls:\n"
        "  2.3.1 Ensure RDS instances are not publicly accessible (PubliclyAccessible=false).\n"
        "  2.3.2 Ensure RDS instances are encrypted at rest (StorageEncrypted=true).\n"
        "  2.3.3 Ensure Enhanced Monitoring is enabled for all production RDS instances.\n"
        "  Note: Use AWS Config rule 'rds-instance-public-access-check' for continuous compliance."
    ),
    ("lambda", "well_architected"): (
        "AWS Well-Architected Framework — Lambda relevant controls:\n"
        "  Security SEC 2 — Use least-privilege execution roles; one role per function.\n"
        "  Security SEC 8 — Protect secrets: use Secrets Manager, not environment variables.\n"
        "  Performance PER 2 — Set appropriate memory and timeout for each function.\n"
        "  Reliability REL 6 — Handle partial failures; use DLQs for async invocations."
    ),
    ("terraform", "well_architected"): (
        "AWS Well-Architected Framework — Terraform/IaC relevant controls:\n"
        "  Ops Excel OPS 5 — Use version-controlled IaC for all infrastructure changes.\n"
        "  Security SEC 1 — Use separate state backends per environment (dev/staging/prod).\n"
        "  Security SEC 8 — Encrypt state files at rest using KMS CMK on S3 backend.\n"
        "  Reliability REL 1 — Enable DynamoDB locking to prevent concurrent state corruption.\n"
        "  Security SEC 2 — Apply least-privilege IAM to the Terraform CI/CD pipeline role."
    ),
    ("kubernetes", "cis"): (
        "CIS Kubernetes Benchmark (EKS) — key controls:\n"
        "  1.1.1 Ensure RBAC is enabled on EKS clusters.\n"
        "  1.2.1 Ensure anonymous authentication to the API server is disabled.\n"
        "  3.1.1 Ensure client certificate authentication is not used for users.\n"
        "  4.2.1 Ensure containers do not run as root (runAsNonRoot: true in securityContext).\n"
        "  4.2.6 Ensure the root filesystem is read-only (readOnlyRootFilesystem: true).\n"
        "  5.2.1 Ensure the cluster has network policies configured.\n"
        "  5.4.1 Prefer secrets stored in AWS Secrets Manager over Kubernetes Secrets."
    ),
}
